sum=lambda a,b:a+b
avg=lambda a,b: (a+b)/2
print(sum(4,5))
print(avg(2,5))

# use lambda function for simple task because we can only write one expression like a+b or (a+b)/2 written on above lambda function
#lambda functions are used in higher order functions 
# higher ordered function are functions which take function as a input or returns a function or both  